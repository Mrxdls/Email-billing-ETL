# Gmail API + Google auth
google-api-python-client>=2.0
google-auth>=2.0

# AWS (S3 raw landing + Secrets Manager)
boto3>=1.34

# Operational Postgres (history_id watermark)
psycopg2-binary>=2.9

# Warehouse (Synapse) — schema + connectivity
SQLAlchemy>=2.0
pyodbc>=5.0
