# Gmail API + Google auth
google-api-python-client>=2.0
google-auth>=2.0

# AWS (S3 + Secrets Manager)
boto3>=1.34

# Operational Postgres
psycopg2-binary>=2.9

# Warehouse (Redshift) — native driver + SQLAlchemy dialect
SQLAlchemy>=2.0
redshift_connector>=2.1
sqlalchemy-redshift>=0.8

# Silver extraction (HTML parsing)
beautifulsoup4>=4.12
